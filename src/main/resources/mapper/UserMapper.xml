<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="demo3.demo3_068.mapper.UserMapper">

    <resultMap id="UserResultMap" type="demo3.demo3_068.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="role" property="role"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByUsername" resultMap="UserResultMap">
        select id, username, email, password, nickname, role, create_time, update_time
        from `user`
        where username = #{username}
    </select>

    <select id="selectByEmail" resultMap="UserResultMap">
        select id, username, email, password, nickname, role, create_time, update_time
        from `user`
        where email = #{email}
    </select>

    <select id="selectById" resultMap="UserResultMap">
        select id, username, email, password, nickname, role, create_time, update_time
        from `user`
        where id = #{id}
    </select>

    <insert id="insert" parameterType="demo3.demo3_068.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        insert into `user` (username, email, password, nickname, role)
        values (#{username}, #{email}, #{password}, #{nickname}, #{role})
    </insert>
</mapper>
