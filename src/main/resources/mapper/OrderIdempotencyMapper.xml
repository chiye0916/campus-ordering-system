<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="demo3.demo3_068.mapper.OrderIdempotencyMapper">

    <resultMap id="OrderIdempotencyResultMap" type="demo3.demo3_068.entity.OrderIdempotency">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="idempotency_key" property="idempotencyKey"/>
        <result column="request_hash" property="requestHash"/>
        <result column="order_id" property="orderId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="demo3.demo3_068.entity.OrderIdempotency"
            useGeneratedKeys="true" keyProperty="id">
        insert into order_idempotency
            (user_id, idempotency_key, request_hash, order_id, status, create_time, update_time)
        values
            (#{userId}, #{idempotencyKey}, #{requestHash}, #{orderId}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectByUserIdAndKey" resultMap="OrderIdempotencyResultMap">
        select id, user_id, idempotency_key, request_hash, order_id, status, create_time, update_time
        from order_idempotency
        where user_id = #{userId}
          and idempotency_key = #{idempotencyKey}
    </select>

    <update id="markSucceeded">
        update order_idempotency
        set order_id = #{orderId},
            status = #{status},
            update_time = #{updateTime}
        where id = #{id}
    </update>

    <update id="markFailed">
        update order_idempotency
        set status = #{status},
            update_time = #{updateTime}
        where id = #{id}
    </update>
</mapper>
